# AI Usage Report
## Assignment 6 - Writing JUnit Test Cases

**Tools Used:** Claude (claude.ai)

**How it was used:**

- Reviewed my test cases for completeness after I had written the initial versions
- Asked for clarification on JUnit 5 syntax (e.g., correct usage of `assertThrows` for `IllegalStateException`)
- Used as a reference to confirm edge case coverage matched the assignment rubric

**What I did independently:**

- Wrote all test cases for every method in `IntegerSet.java` based on my own HW5 implementation
- Identified edge cases by reading the assignment spec (duplicate adds, empty set exceptions, identical set diff, etc.)
- Structured the test file and package layout myself
- Verified all tests compiled and ran correctly in VS Code
